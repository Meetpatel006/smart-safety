import 'expo-router/entry'

// Expo Router will use the files under the `app/` folder to define navigation.
// The actual providers, splash handling and notification setup are moved into
// `app/_layout.tsx` so that the file-based routing can control the navigator.

export default function App() {
  // The expo-router entry manages the native navigation container. Keep App minimal.
  return null
}
